{
  "dockerFile": "Dockerfile",
  "runArgs": [
    "--init",
    "--privileged"
  ],
  "settings": {
    "terminal.integrated.shell.linux": "/bin/bash"
  },
  "extensions": [
    "ms-iot.vscode-ros"
  ],
  "postCreateCommand": "sudo apt update && sudo apt install -y ros-humble-desktop"
}
